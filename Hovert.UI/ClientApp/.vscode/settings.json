{
    "files.exclude": {
        "**/*.js": { "when": "$(basename).ts"},
        "**/*.js.map": { "when": "$(basename)"}
    },
    "search.exclude": {
        "**/*.js": true
    },
    "json.maxItemsComputed": 9000
}